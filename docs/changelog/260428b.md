# Changelog: 2026-04-28 — RadEval 2.2.0 (TRL reward hardening)

**Branch**: `trl` → PR into `hopprai/RadEval:main`.

This changelog documents the v2.1.0 → 2.2.0 change that positions RadEval
as a TRL-compatible *reward provider* and closes a confirmed F1CheXbert
`KeyError` bug in `make_reward_fn`. It lives under `docs/changelog/` which
is stripped by `scripts/publish_public.py` and never reaches
`jbdel/RadEval`.

## Purpose

RadEval already exported `make_reward_fn` as a TRL-compat wrapper, but the
surface was untested against a real `GRPOTrainer`, had a latent bug on
every multi-key-per-sample metric (F1CheXbert was the confirmed offender),
and shipped a stub `examples/trl_reward_demo.py` that never instantiated
TRL. v2.2.0 hardens the reward path, adds a runnable GRPO quickstart, and
reframes the documentation to match the tested surface.

## Scope

**Shipped:**

- `RadEval/rewards.py` rewritten (~190 LOC vs ~50 LOC prior):
  - `make_reward_fn(metric, reference_column=..., key=..., score_transform=..., **metric_kwargs)`.
  - Required `key=` when the underlying metric's `compute(..., per_sample=True)`
    returns >1 key. `ValueError` with the valid-keys list otherwise. Fixes
    the F1CheXbert case; also surfaces ROUGE, RadGraph similarly.
  - Defensive conversational-completion handling: `list[str]` passes
    through; `list[list[dict]]` extracts `content` from the last assistant
    message (OpenAI-style best-effort heuristic, fires `UserWarning` per
    call, filterable via `warnings.simplefilter`). Missing `role`/`content`
    keys or unrecognized shapes raise `TypeError` with upstream-preprocess
    guidance.
  - `validate_rewards(values, metric_name)` helper: passes `None` entries
    unchanged (TRL sample-skip convention), normalizes numpy scalars and
    0-D / 1-element 1-D torch tensors via `.item()`, raises `ValueError`
    on NaN/Inf naming metric and sample index.
  - Absorbs TRL-forwarded kwargs (`prompts`, `completion_ids`,
    `trainer_state`, `log_extra`, `log_metric`, `environments`) plus
    arbitrary extra dataset columns via `**kwargs`.
  - `validate_rewards` re-exported from `RadEval/__init__.py`.
- `setup.py`: version 2.1.0 → 2.2.0; added `extras_require['rl'] = ['trl>=1.3.0,<2']`.
- `examples/trl_grpo_quickstart.py`: replaces the stub `trl_reward_demo.py`.
  Runs 5 GRPO steps against `Qwen/Qwen2.5-0.5B` using
  `make_reward_fn("bleu")` on a 20-row synthetic fixture.
- `examples/trl_reward_demo.py`: deleted (was a call-shape print stub).
- `tests/fixtures/synthetic_reports.json`: new 20-row hand-authored
  fixture of (prompt, ground_truth) pairs for chest-x-ray reports.
  `.gitignore` updated with a targeted exception
  (`!tests/fixtures/synthetic_reports.json`) so the committed source
  isn't caught by the `tests/**/*.json` generated-snapshot rule.
- `tests/test_rewards.py`: new unit test file covering lightweight-metric
  round-trips (BLEU / ROUGE / BERTScore), F1CheXbert multi-key behavior,
  ROUGE multi-key behavior, conversational `list[list[dict]]` handling,
  missing-key/unknown-shape `TypeError`, `score_transform` composition
  and clipping idiom, `validate_rewards` edge cases (numpy/torch scalars,
  1-element 1-D tensors, NaN/Inf, `None` pass-through), TRL kwargs
  absorption with extra dataset columns, `UserWarning` on API-based
  metrics, empty-completions input. 26 tests.
- `tests/test_trl_integration.py`: new smoke test. `pytest.importorskip("trl")`.
  One 1-step GRPO run on `trl-internal-testing/tiny-Qwen2ForCausalLM-2.5`
  (~2.4M params) wrapping `make_reward_fn("bleu")` through a local
  reward fn that logs invocation count. Asserts `global_step == 1` and
  that the reward fn was actually called.
- `tests/test_e2e.py`: removed the three pre-existing reward tests
  (`test_make_reward_fn`, `test_reward_fn_score_transform`,
  `test_reward_fn_custom_column`) — now subsumed by `test_rewards.py`.
- `docs/trl_rewards.md`: rewritten. GRPO is the primary tested path;
  RLOO / PPO / VLM / DPO live in a single "adjacent / untested uses"
  section. Documents the reward callable contract (batch semantics,
  `**kwargs` absorption, required `key=`, `None` pass-through, NaN/Inf
  raise). Adds a VRAM callout for duplicate heavy-metric loads. Adds
  a "known limitations" section for the conversational heuristic and
  RadCliQ per-sample cost. Speed table updated: RadGraph moved to
  medium tier, stronger warning on GREEN, API metrics marked "not
  practical for online RL." Pins the TRL/transformers/torch known-good
  stack.
- `README.md`: version badge bumped; RL section updated with
  `pip install RadEval[rl]`, BLEU default example, and quickstart link;
  known-good-stack note calls out `trl>=1.3.0,<2` for the `[rl]` extras.

**Explicitly deferred (to a future PR, contingent on demand):**

- Scorer caching. Removed from this PR after multiple external reviewers
  flagged memory / DDP / unhashable-kwargs concerns. Revisit with
  `functools.lru_cache` on an internal factory if a real workflow demands it.
- `CompositeReward` class. TRL's native `reward_funcs=[...]` +
  `reward_weights=[...]` already covers weighted composition with
  per-function auto-logging.
- Committed VLM example file. Docs-only for now; TRL's own
  `grpo_vlm.py` stays canonical.
- `log_extra` / `log_metric` forwarding.
- Async reward-fn support.

## Verification

Tested environment: `/nfs/cluster/miniconda3/envs/radeval-t5`
(transformers 5.6.2, torch 2.9.1+cu128, tokenizers 0.22.2, trl 1.3.0).
Per [260428.md] this is the known-good RadEval 2.1+ stack; transformers
4.x envs are out of scope (RadEval 2.1+ requires transformers>=5.0).

- `pytest tests/test_rewards.py` → **26 passed** in ~53s.
- `pytest tests/test_rewards.py tests/test_e2e.py` → **40 passed** in ~52s.
- `pytest tests/test_trl_integration.py` → **1 passed** in ~67s (with
  NCCL bind warning to stderr — unrelated, the training step completes
  cleanly).
- `python examples/trl_grpo_quickstart.py` — 5 GRPO steps complete in
  ~13s on the available GPU, reward trajectory prints non-empty.

## Decisions captured (one-liners)

- **Require `key=` on multi-key metrics** (no implicit default). The
  generic check is ~3 LOC over a F1CheXbert-only patch and avoids
  re-touching `rewards.py` for the next such metric. ROUGE and RadGraph
  also need it — they fail loudly with the valid-keys list printed.
- **`validate_rewards` passes `None` through unchanged.** TRL uses
  `None` to skip samples in multi-task routing; coercing it via
  `float()` would crash the training loop.
- **Use `.item()` before `float()`.** Catches 0-D and 1-element 1-D
  torch tensors without a `float()` `ValueError` on some PyTorch
  versions.
- **`warnings.warn(..., UserWarning)` on the conversational heuristic**,
  not a module-level "once-only" flag with `logging.info`. User-
  filterable via standard Python; no global state under DDP.
- **No `clip_range` argument.** `score_transform=lambda x: max(lo, min(hi, x))`
  expresses it without API-surface growth or ambiguous ordering.
- **No `default_on_error`.** Silent zero-rewards hide tokenizer /
  vocab / edge-case bugs in RL loops where the reward is the only
  error signal.
- **Paper-RadCliQ positioning**: "recommended for eval / final-tune;
  benchmark per-sample cost before using for online training." No
  invented `radcliq_v0`-style composite with internal weights.
- **Qwen/Qwen2.5-0.5B in the user-facing quickstart; tiny random model
  in the CI integration test.** The 0.5B model would risk OOM or long
  downloads on a CPU CI runner; the tiny random model (~2.4M params)
  is fast and deterministic.

## Follow-up

- v2.2.1: if a user reports a real workflow that needs scorer caching,
  add it as `functools.lru_cache` on an internal factory.
- v2.3: if a user reports a real workflow that needs `log_extra` /
  `log_metric` forwarding or composite rewards with per-sample
  diagnostics, add then.

## Publish-script audit

- No new private metrics, registry entries, or test files under the
  `# --- PRIVATE METRICS ---` marker block. `scripts/publish_public.py`
  path unchanged.
- `docs/changelog/260428b.md` (this file) is under `docs/changelog/`
  which is already in `PRIVATE_DIRS`.
- No leak-scan patterns in the new code.
