i'd like to add trl support (https://github.com/huggingface/trl) to this radeval library. traditionally, this library was built for simply evaluating outputs. i instead would like to make it amenable to using reinforcement learning, e.g. grpo et al.

to accomplish this, i want you to create an agent team:
1) a trl expert: familiarize yourself with the trl library
2) a hoppr/rl expert: familiarize yourself with ~/worktrees/hopprai/dvv/python/ml/models/vlm_align, which currently uses similar metrics (maybe not radeval directly, though) to improve radiology report generation metrics on a set of metrics. NOTE that this repository implements a custom grpotrainer because huggingface didn't have an obvious trainer for taking vision as input (all there grpo stuff is geared at llm's only). we could decide to include this implementation, i.e. grpo for a vlm, although intuitively with grpo you'd only want to adapt the language model anyway, and you'd probably keep the vision encoder itself frozen? anyway, consider what's reasonable and what is available in this hoppr/rl implementation / how it uses radeval
3) a radeval expert, i.e. the existing module. note that we currently have a basic rl example, but it's very hypothetical

after becoming experts in their respective codebases, the three experts should confer as to what would be the most compelling contribution to radeval which enables users to leverage the metrics for rl. we want this to be reasonable and practical.

please make a proposal about what contributions we could make to radeval to incorporate standard hf libraries (e.g., trl) so that this module is usable in radiology rl tasks. note that we will want to have some tangible examples etc. after making this feature change, we will make an announcement about the updates to the library and as such want the final output to be polished and thorough.
