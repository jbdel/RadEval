I’d like to explore adding Hugging Face TRL support to the `radeval` library: https://github.com/huggingface/trl

Historically, `radeval` has focused on evaluating generated radiology reports. I want to make it more useful for reinforcement learning workflows, especially GRPO-style training where radiology report metrics are used as rewards.

Please approach this as a design/proposal task, not immediate implementation. Your output should be a document: plan/03.md

Create three “expert” perspectives and have them independently investigate, then synthesize a recommendation:

1. TRL expert
   - Study the current TRL API and examples.
   - Identify what TRL supports well out of the box for reward functions, GRPO, PPO, custom trainers, and multimodal/VLM inputs.
   - Clarify where TRL is LLM-only versus extensible to VLMs.

2. HOPPR RL / `vlm_align` expert
   - Review `~/worktrees/hopprai/dvv/python/ml/models/vlm_align`.
   - Understand how it currently uses radiology metrics as rewards for report generation.
   - Pay special attention to the custom GRPO trainer, why it exists, how it handles vision inputs, and whether/how it uses `radeval`.
   - Assess whether pieces of this implementation should inform or be generalized into `radeval`.

3. `radeval` expert
   - Review the existing `radeval` codebase, APIs, examples, and current RL example.
   - Identify the cleanest extension points for RL support without overcomplicating the library.
   - Evaluate whether the current hypothetical RL example should be replaced, expanded, or reframed.

After the investigation, have the three perspectives confer and produce a polished proposal.

The proposal should answer:

- What is the most practical and compelling contribution `radeval` can make to RL workflows?
- Should `radeval` provide:
  - TRL-compatible reward functions?
  - example GRPO/PPO training scripts?
  - wrappers/adapters around existing metrics?
  - VLM-specific trainer utilities?
  - documentation only?
- Where should we draw the boundary between `radeval` and downstream training code?
- How should we support LLM-only report generation versus VLM report generation?
- Should we incorporate or reference ideas from `vlm_align`’s custom GRPO trainer?
- What concrete examples should ship with the feature?
- What API changes, if any, are needed?
- What would make this feature announcement-worthy while still being maintainable?

Please make the recommendation practical and scoped. I do not want a sprawling RL framework inside `radeval`; I want the smallest set of high-leverage additions that make `radeval` clearly useful for radiology RL with standard HF tooling.

Final output format:

1. Executive summary
2. Findings from each expert
3. Recommended scope
4. Proposed API / code structure
5. Example workflows to include
6. Implementation plan
7. Risks / tradeoffs
8. Announcement positioning
