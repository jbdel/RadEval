edits:

readme
- change section from "usage" to "usage: evaluation" and "rl w radeval rewards" to "usage: rl rewards"

in docs/trl_rewards*, add a table of contents (depth 2) at the bottom of the existing intro section
